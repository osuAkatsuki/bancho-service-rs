name: production-deploy

on:
  push:
    branches:
      - main
      - validate-network-adapters

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  production-deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Check out latest commit
        uses: actions/checkout@v4

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata (tags, labels) for Docker
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ghcr.io/osuakatsuki/bancho-service-rs

      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./Dockerfile
          push: true
          tags: |
            ghcr.io/osuakatsuki/bancho-service-rs:latest
            ghcr.io/osuakatsuki/bancho-service-rs:${{ github.sha }}
          labels: ${{ steps.meta.outputs.labels }}

      - name: Deploy to production
        uses: appleboy/ssh-action@v1
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: root
          key: ${{ secrets.SERVER_SSH_KEY }}
          envs: GHCR_TOKEN
          script: |
            echo "$GHCR_TOKEN" | docker login ghcr.io -u github-actions --password-stdin
            cd /opt/akatsuki
            docker compose pull bancho-service-rs-api bancho-service-rs-pubsub-daemon bancho-service-rs-cleanup-cron
            docker compose up -d bancho-service-rs-api bancho-service-rs-pubsub-daemon bancho-service-rs-cleanup-cron
        env:
          GHCR_TOKEN: ${{ secrets.GITHUB_TOKEN }}
